/**
 * @mongodbSidebar
 */

import type { TsidebarItem } from '../../types/global'

export const mongodb = (): TsidebarItem[] => {
  return [
    {
      text: 'MongoDB',
      items: [
        {
          text: '',
          link: ''
        }
      ]
    }
  ]
}
